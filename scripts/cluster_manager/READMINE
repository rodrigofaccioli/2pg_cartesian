Here contais scripts for cluster manager

* nodes.txt This file represents name of nodes. Your sintaxe must be user@nodename. 
	Example: root@wefold01
	See nodes.txt file in this directory 

* multscp runs scp command in all nodes that are described at nodes.txt. multscp <SOURCE> <DEST>. 
	<SOURCE> means path which will be copied to all nodes inserted at nodes.txt
	<DEST> means where will be saved in destination
	Example: ./multscp "2pg_cartesian/" "/root/programs/"

* multssh runs ssh command in all nodes that are described at nodes.txt. 
	Example: ./multssh "kill -9 -1"

